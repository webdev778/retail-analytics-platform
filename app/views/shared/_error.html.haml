- if notice.present?
  .alert.alert-info.alert-dismissible
    %button.close{ data: { dismiss: 'alert' } } ×
    = notice
- if alert.present?
  .alert.alert-danger
    %button.close{ data: { dismiss: 'alert' } } ×
    = alert